declare global {
  namespace App {
    // interface Error {}
    interface Locals {
      athlete: string;
      accessToken: string;
    }
    // interface PageData {}
    // interface Platform {}
  }
}

export {};
